SELECT
  *
FROM
  flights
WHERE
  airline_name LIKE '%{{Table1.searchText}}%'
ORDER BY
  {{Table1.sortOrder.column || 'flight_no'}} {{Table1.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Table1.pageSize}}
OFFSET
  {{Table1.pageOffset}}